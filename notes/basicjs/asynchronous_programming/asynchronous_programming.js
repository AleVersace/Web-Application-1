/* 
JavaScript is single-threaded 
Callbacks are fundamental for asynchronous JS (not concurrent!)
setting a timeout, for example, with the engine help.
Execution is not procedural, must be careful to create
non-blocking code.
*/


/* Executed commands to manage node packages of the project:
Shell:
npm init
npm install sqlite3  (to install on the project, not global)

if we remove the node_modules and the package-lock.json
we can still retreive all from the package.json doing "npm install"
*/

